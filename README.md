# 🚀 TesteAutomatizado_QAPlayground_Cypress

Este template oferece um guia abrangente para configurar, executar e compreender o projeto de automação de testes construído com Cypress.

### 🌟 Visão Geral

Este projeto utiliza o **Cypress** para simular interações do usuário em um ambiente de navegador real.

### 🛠️ Pré-requisitos

Antes de começar, certifique-se de ter o seguinte instalado em seu sistema:

- **Node.js**: Versão 22.
- **npm**: Geralmente já vem instalado com o Node.js.

### ⚙️ Instalação e Configuração

1. **Clonar o Repositório:**

    ```bash
    git clone 
    cd /TesteAutomatizado_QAPlayground_Cypress
    ```

2. **Instalar Dependências:**

    Instale as dependências do projeto, incluindo o Cypress e quaisquer outras bibliotecas necessárias.

    ```bash
    npm install
    # ou
    yarn install
    ```

### ▶️ Executando os Testes

Você tem várias opções para executar os testes.

#### 1. Cypress Test Runner (Modo Interativo)

Use este modo para **desenvolvimento, depuração e visualização** dos seus testes enquanto você os escreve.

```bash
npm run cypress:open
# ou
npx cypress open
```